import React from 'react';
import { LogotypeIcon } from './Logotype.styled';

const Logotype = () => {
    return (
        <LogotypeIcon to="/">
          Logotype
        </LogotypeIcon>
    );
};

export default Logotype;
